input-file-path = "orders"

db-config {
  driver = "org.postgresql.Driver"
  url = "jdbc:postgresql://localhost:5432/mt_experiments"
  user = "postgres"
  password = "postgres"
}

enabled-pipelines = ${?ENABLED_PIPELINES}

flows-config {
    parallelism = 12
    partitions-count = 12
}

